import { createSlice } from '@reduxjs/toolkit'

const crmStore = createSlice({
  name: "crm",
  initialState: {
  },
  reducers: {
  }
})

const crmReducer = crmStore.reducer

export default crmReducer